{
  "name": "mia-inspiration-shipping",
  "version": "1.0.0",
  "description": "מערכת ניהול משלוחים - מייה אינספיריישן",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server",
    "start": "node server/src/index.js"
  }
}
