# הוראות לקלוד — פרויקט mia-inspiration-shipping

הקובץ הזה מכוון את קלוד איך לעבוד בפרויקט הזה ספציפית. הוא משלים את ה-CLAUDE.md הראשי של מרחב העבודה.

---

## 🎯 הקשר

פרויקט גמר אקדמי, צוות של 3 סטודנטיות במערכות מידע ברמה בסיסית. הקוד צריך להיות **פשוט, מובן וקריא לשלוש חברות הצוות**, לא רק לכותבת.

## 📚 מקורות מידע חשובים

- **העבודה התאורטית:** `C:\Users\edriz\OneDrive\שולחן העבודה\שנה ג\פרקטיקום\פרקטיקום- חלק ב סופי.docx`
- **תיעוד API של אוריין:** `C:\Users\edriz\Downloads\fw1363866360760.zip` + העתק יישמר ב-`docs/orian-api.md`
- **דוגמאות XML:** באותו ZIP — `orian_test_Create_Transportation_*.xml`

## ✅ עקרונות כתיבה

### קוד
- **JavaScript בלבד** (לא TypeScript) — שפה אחת לצוות שלם
- **שמות משתנים באנגלית, הערות בעברית** במקומות מורכבים
- **פשטות לפני אלגנטיות** — אם יש שתי דרכים, נבחר את הברורה גם אם ארוכה יותר
- **בלי קוד "חכם" מדי** — בלי one-liners מתוחכמים, בלי decorators מתקדמים

### Commits
- בעברית, פשוטים: `הוספה: מסך התחברות` / `תיקון: באג בטופס משלוח` / `עדכון: README`
- כל commit מטפל בדבר אחד הגיוני

### תקשורת בעברית
- כל הסבר וטקסט-משתמש — בעברית
- שמות endpoints, components, פונקציות — באנגלית
- **כשעולה מושג טכני חדש — להסביר אותו פעם אחת בעברית** לפני שמשתמשים בו

## 🔒 כללי אבטחה (חשוב!)

- **לעולם לא** לשמור credentials של אוריין בקוד או ב-commit
- כל סוד יושב ב-`server/.env` (שכבר ב-`.gitignore`)
- יש קובץ דוגמה `server/.env.example` שמראה אילו משתנים נדרשים — בלי הערכים האמיתיים
- **לפני כל commit** — לוודא שלא נכנסו לקבצים: סיסמאות, tokens, מפתחות, מספרי לקוח אמיתיים

## 🧭 שגרת עבודה

- לפני יצירת קובץ חדש: לבדוק אם יש מקום קיים שמתאים
- לפני התקנת ספרייה חדשה: להציע ולהסביר למה היא נחוצה
- לפני שינוי באותרכיטקטורה: להציע ב-`/plan`

## 📦 מבנה הפרויקט

```
mia-inspiration-shipping/
├── client/                 # Frontend - React + Vite
│   └── src/
│       ├── pages/          # מסכים מלאים
│       ├── components/     # רכיבים קטנים לשימוש חוזר
│       ├── api/            # פונקציות שמדברות עם השרת שלנו
│       └── hooks/          # React hooks מותאמים
├── server/                 # Backend - Node.js + Express
│   └── src/
│       ├── routes/         # ה-API שלנו
│       ├── services/
│       │   └── orian/      # התקשורת עם API של אוריין (XML)
│       ├── db/             # SQLite
│       └── middleware/     # Auth, error handling
└── docs/                   # תיעוד טכני
```

## 🌐 משתני סביבה (server/.env)

```
PORT=3001
ORIAN_BASE_URL=https://disapiTest.orian.com
ORIAN_USERNAME=...
ORIAN_PASSWORD=...
ORIAN_CONSIGNEE=...
JWT_SECRET=...
DATABASE_PATH=./data/mia.db
```

## 🎨 כללי UI

- **RTL מלא** בכל המסכים
- **Mobile-First** — מתחילים מ-375px
- **כל מסך = פעולה אחת ראשית** (Single Action Screen)
- **משוב מיידי** לכל פעולה
- צבעי סטטוס: כחול=`נשלח`, ירוק=`התקבל`, כתום=`אי-התאמה`
